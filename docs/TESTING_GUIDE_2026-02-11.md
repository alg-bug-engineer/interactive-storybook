# 双服务架构测试指南

**日期**: 2026-02-11
**版本**: 1.0

---

## 📋 测试前准备

### 1. 安装依赖

```bash
cd backend
pip install volcengine-python-sdk websockets
```

### 2. 配置环境变量

编辑 `.env` 文件，填写火山 API 凭证：

```bash
# 火山即梦官方 API
VOLCANO_JIMENG_AK=your_access_key_here
VOLCANO_JIMENG_SK=your_secret_key_here

# 火山 TTS 官方 API
VOLCANO_TTS_APPID=your_app_id_here
VOLCANO_TTS_ACCESS_TOKEN=your_access_token_here
```

### 3. 启动服务

```bash
# 后端
cd backend
uvicorn app.main:app --reload --host 0.0.0.0 --port 1001

# 前端
cd frontend
npm run dev
```

---

## 🧪 测试用例

### 测试 1: 未登录用户流程

**目标**: 验证未登录用户使用本地服务

**步骤**:
1. 清除浏览器缓存和 localStorage
2. 访问 http://localhost:1000
3. 点击"开始新故事"
4. 观察图片和音频生成

**预期结果**:
- 图片生成：使用本地 jimeng-api
- TTS 生成：使用 edge-tts
- 后端日志显示：`[图片生成] 服务类型: 本地服务, 用户: 未登录`
- 后端日志显示：`[TTS生成] 服务类型: 本地服务, 用户: 未登录`

**验证日志**:
```bash
# 查看后端日志
tail -f backend/logs/app.log | grep "服务类型"
```

---

### 测试 2: 免费用户流程

**目标**: 验证免费用户使用本地服务

**步骤**:
1. 注册新用户
   ```bash
   curl -X POST http://localhost:1001/api/auth/register \
     -H "Content-Type: application/json" \
     -d '{"email":"free@example.com","password":"123456"}'
   ```
   保存返回的 token

2. 使用 token 创建故事
   ```bash
   curl -X POST http://localhost:1001/api/story/start \
     -H "Content-Type: application/json" \
     -H "Authorization: Bearer YOUR_TOKEN" \
     -d '{"theme":"小兔子找萝卜","total_pages":5}'
   ```

3. 观察后端日志

**预期结果**:
- 图片生成：使用本地 jimeng-api
- TTS 生成：使用 edge-tts
- 后端日志显示：`[图片生成] 服务类型: 本地服务, 用户: free@example.com`
- 用户数据文件显示 `is_paid: false`

**验证命令**:
```bash
# 检查用户数据
ls -la backend/data/users/
cat backend/data/users/*.json | jq .
```

---

### 测试 3: 付费用户流程

**目标**: 验证付费用户使用官方 API

**步骤**:
1. 注册并登录用户
2. 升级为付费用户
   ```bash
   curl -X POST http://localhost:1001/api/auth/upgrade \
     -H "Authorization: Bearer YOUR_TOKEN"
   ```

3. 创建新故事
   ```bash
   curl -X POST http://localhost:1001/api/story/start \
     -H "Content-Type: application/json" \
     -H "Authorization: Bearer YOUR_TOKEN" \
     -d '{"theme":"小猫咪探险","total_pages":5}'
   ```

4. 观察后端日志

**预期结果**:
- 图片生成：使用火山即梦官方 API
- TTS 生成：使用火山 TTS 官方 API
- 后端日志显示：`[图片生成] 🚀 使用官方火山即梦 API（付费用户）`
- 后端日志显示：`[TTS生成] 🚀 使用官方火山 TTS API（付费用户）`
- 生成速度明显快于本地服务

**性能对比**:
```bash
# 查看耗时统计
tail -f backend/logs/app.log | grep "耗时"
```

---

### 测试 4: 缓存命中测试

**目标**: 验证图片缓存优化生效

**步骤**:
1. 创建第一个故事（主题：小狗找骨头）
2. 等待所有图片生成完成
3. 创建第二个故事（主题：小狗找骨头，相同主题）
4. 观察后端日志

**预期结果**:
- 第二个故事的图片生成速度明显加快
- 后端日志显示：`[图片缓存] 检查: ✅ 命中`
- 不会重复调用 API 生成相同图片

**验证缓存**:
```bash
# 查看缓存映射文件
cat backend/data/image_cache/cache_map.json | jq .

# 查看缓存图片
ls -lh backend/data/images/
```

**缓存统计**:
```python
# Python 脚本检查缓存统计
from app.utils.image_cache import get_cache_stats
stats = get_cache_stats()
print(stats)
# 输出示例：
# {
#   "total_entries": 10,
#   "valid_entries": 10,
#   "invalid_entries": 0,
#   "total_size_mb": 15.3
# }
```

---

### 测试 5: 降级策略测试

**目标**: 验证官方 API 失败时自动降级到本地服务

**步骤**:
1. 暂时修改 `.env` 文件，设置错误的火山 API 凭证
   ```bash
   VOLCANO_JIMENG_AK=invalid_key
   VOLCANO_JIMENG_SK=invalid_secret
   ```

2. 重启后端服务
3. 使用付费用户创建故事
4. 观察后端日志

**预期结果**:
- 尝试调用官方 API 失败
- 后端日志显示：`[图片生成] ⚠️ 官方 API 失败，尝试降级到本地服务`
- 自动切换到本地服务，故事仍然成功生成
- 前端用户无感知，正常使用

**恢复步骤**:
```bash
# 恢复正确的凭证
# 编辑 .env 文件
# 重启后端服务
```

---

### 测试 6: TTS 音频生成测试

**目标**: 验证不同用户使用不同 TTS 服务

**步骤**:
1. 创建故事并翻到第一页
2. 点击播放音频
3. 观察后端日志

**免费用户预期**:
- 后端日志：`[TTS生成] 🐌 使用 edge-tts 服务（免费用户）`
- 音频文件路径：`data/audio/tts/story_xxx_0_zh-CN-XiaoxiaoNeural.mp3`

**付费用户预期**:
- 后端日志：`[TTS生成] 🚀 使用官方火山 TTS API（付费用户）`
- 音频文件路径：`data/audio/volcano_tts/story_xxx_0_BV700_V2.mp3`

**验证音频文件**:
```bash
# edge-tts 音频
ls -lh backend/data/audio/tts/

# 火山 TTS 音频
ls -lh backend/data/audio/volcano_tts/
```

---

### 测试 7: 音频预加载测试

**目标**: 验证音频预加载优化生效

**步骤**:
1. 创建一个多页故事
2. 播放第一页音频
3. 观察后端日志
4. 翻到第二页
5. 观察第二页音频加载速度

**预期结果**:
- 第一页播放时，后台预生成第二页音频
- 后端日志显示：`[TTS生成] 开始生成新音频: story=xxx, segment=1`
- 翻到第二页时，音频立即播放（使用缓存）
- 后端日志显示：`[TTS生成] ✅ 使用缓存`

---

### 测试 8: 完整用户体验流程

**目标**: 验证完整的用户体验流程

**步骤**:
1. 未登录状态创建故事（本地服务）
2. 注册并登录（免费用户）
3. 继续故事或创建新故事（本地服务）
4. 升级为付费用户
5. 创建新故事（官方 API）
6. 对比生成速度和质量

**性能对比表**:

| 指标 | 免费用户 | 付费用户 | 提升 |
|------|---------|---------|------|
| 图片生成速度 | ~10s | ~3-5s | 2-3x |
| TTS 生成速度 | ~2s | ~1s | 2x |
| 稳定性 | 一般 | 优秀 | ++ |
| 缓存命中率 | 30% | 30% | 相同 |

---

## 📊 日志检查清单

### 必须出现的日志

**服务调用**:
```
[图片生成] 服务类型: 官方API/本地服务, 用户: xxx
[TTS生成] 服务类型: 官方API/本地服务, 用户: xxx
```

**缓存检查**:
```
[图片缓存] 检查: ✅ 命中 / ❌ 未命中, 缓存键: xxx
[音频缓存] 检查: ✅ 命中 / ❌ 未命中, 缓存键: xxx
```

**生成结果**:
```
[图片生成] ✅ 生成完成，耗时: X.XXs, 路径: xxx
[TTS生成] ✅ 生成完成，耗时: X.XXs, 路径: xxx
```

**降级警告**:
```
[图片生成] ⚠️ 官方 API 失败，尝试降级到本地服务
[图片生成] ✅ 降级到本地服务成功
```

---

## 🐛 常见问题排查

### 问题 1: 火山 API 调用失败

**症状**: 后端日志显示火山 API 认证失败

**排查步骤**:
1. 检查 `.env` 文件中的凭证是否正确
2. 验证凭证是否过期
3. 检查网络连接
4. 查看火山控制台是否有错误

**解决方案**:
```bash
# 重新获取凭证
# 更新 .env 文件
# 重启后端服务
```

---

### 问题 2: 缓存未命中

**症状**: 相同内容重复生成，缓存未命中

**排查步骤**:
1. 检查缓存映射文件是否存在
   ```bash
   cat backend/data/image_cache/cache_map.json
   ```
2. 检查 prompt 是否完全相同
3. 检查 style_id 是否一致

**解决方案**:
```python
# 清空缓存重新测试
from app.utils.image_cache import clear_cache
count = clear_cache()
print(f"清理了 {count} 条缓存")
```

---

### 问题 3: 音频无法播放

**症状**: 前端音频播放失败

**排查步骤**:
1. 检查音频文件是否生成
   ```bash
   ls -lh backend/data/audio/tts/
   ls -lh backend/data/audio/volcano_tts/
   ```
2. 检查文件大小（应该 > 0）
3. 检查文件格式（应该是 .mp3）
4. 检查后端日志是否有错误

**解决方案**:
```bash
# 手动播放音频文件测试
mpg123 backend/data/audio/tts/xxx.mp3
```

---

### 问题 4: 降级未触发

**症状**: 官方 API 失败时未自动降级

**排查步骤**:
1. 检查错误日志
2. 查看是否抛出异常
3. 验证降级逻辑是否执行

**调试代码**:
```python
# 在 image_generation_service.py 中添加调试日志
logger.info(f"[DEBUG] Tier: {tier}, User: {user}")
logger.info(f"[DEBUG] API call result: {result}")
```

---

## ✅ 测试完成检查清单

- [ ] 未登录用户使用本地服务
- [ ] 免费用户使用本地服务
- [ ] 付费用户使用官方 API
- [ ] 缓存命中率 > 0%
- [ ] 降级策略正常工作
- [ ] TTS 音频正常播放
- [ ] 音频预加载正常工作
- [ ] 日志清晰完整
- [ ] 性能提升明显（付费用户）
- [ ] 前端用户体验流畅

---

## 📈 性能基准测试

### 测试脚本

```python
import time
import asyncio
from app.services.image_generation_service import generate_story_image
from app.models.story import Character

async def benchmark_image_generation(user_type: str, user: dict = None):
    """性能基准测试"""
    characters = [
        Character(name="小兔", appearance="white fluffy bunny"),
    ]

    start = time.time()

    # 生成5张图片
    for i in range(5):
        await generate_story_image(
            scene_description=f"Test scene {i}",
            characters=characters,
            emotion="happy",
            style_id="q_cute",
            user=user,
        )

    elapsed = time.time() - start
    avg = elapsed / 5

    print(f"{user_type} 用户:")
    print(f"  总耗时: {elapsed:.2f}s")
    print(f"  平均耗时: {avg:.2f}s/张")
    print(f"  吞吐量: {5/elapsed:.2f}张/秒")

# 运行测试
asyncio.run(benchmark_image_generation("免费", user={"is_paid": False}))
asyncio.run(benchmark_image_generation("付费", user={"is_paid": True}))
```

---

**测试负责人**: _____________________
**测试日期**: _____________________
**测试结果**: ✅ 通过 / ❌ 失败
**备注**: _____________________
